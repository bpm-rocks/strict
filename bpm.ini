[package]
name=strict
version=1.0.0
include=libstrict
description=Enable a "strict mode" for Bash and show call stacks when errors occur.

[dependencies]
assign=*

[devDependencies]
embed-docs=*
unittest=*

[scripts]
readme=embed-docs README.md libstrict
test=unittest test/*.test
